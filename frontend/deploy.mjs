import { createClient } from 'genlayer-js'; import { studionet } from 'genlayer-js/chains'; import { privateKeyToAccount } from 'viem/accounts'; import fs from 'fs'; const account = privateKeyToAccount('0x5f5babe2057032ab30b8a353f13341478785069644a0c6d3126539104cd48168'); const client = createClient({ chain: studionet, account }); async function main() { const code = fs.readFileSync('../contracts/grant_auditor.py', 'utf8'); console.log('Deploying...'); const hash = await client.deployContract({ code, args: [] }); console.log('Deploy tx hash:', hash); const receipt = await client.waitForTransactionReceipt({ hash }); console.log('Contract Address:', receipt.data.contract_address); } main().catch(console.error);

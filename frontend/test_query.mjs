import { createClient } from 'genlayer-js'; import { studionet } from 'genlayer-js/chains'; const CONTRACT_ADDRESS = '0xEE6FaaA2F62deA104D0Ba89FD521e78a92c31ff0'; async function main() { const client = createClient({ chain: studionet }); try { const rawJson = await client.readContract({ address: CONTRACT_ADDRESS, functionName: 'get_all_grants', args: [] }); console.log('RAW JSON:', rawJson); } catch(e) { console.error('ERROR:', e); } } main();

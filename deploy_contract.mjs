import { createClient } from 'genlayer-js'; import { studionet } from 'genlayer-js/chains'; import { createWalletClient, custom } from 'viem'; import { privateKeyToAccount } from 'viem/accounts'; import fs from 'fs'; async function main() { const account = privateKeyToAccount('0x5f5babe2057032ab30b8a353f13341478785069644a0c6d3126539104cd48168'); const client = createClient({ chain: studionet, account }); const code = fs.readFileSync('contracts/grant_auditor.py', 'utf8'); try { const txHash = await client.deployContract({ code, args: [] }); console.log('HASH:', txHash); const receipt = await client.waitForTransactionReceipt({ hash: txHash }); console.log('RECEIPT:', receipt); } catch(e) { console.error('ERROR:', e); } } main();
